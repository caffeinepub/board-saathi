{
  "frontend/src/App.tsx": "Main application router setup with all routes including the new `/pro` route for the ProUpgradePage, using TanStack Router with authenticated layout wrapper.",
  "frontend/src/pages/SubjectsPage.tsx": "Subjects listing page showing all 6 CBSE Class 10 subjects as clickable cards with chapter completion progress bars and color-coded subject icons.",
  "frontend/src/pages/ChaptersPage.tsx": "Chapter management page for a subject with add/complete chapter functionality and expandable notes/questions tabs; fixed `weightage` to pass a `number` instead of `bigint` to `useAddChapter`.",
  "frontend/src/components/NotesSection.tsx": "Chapter notes component supporting text notes and camera/file image capture, displaying notes with image preview toggle and delete functionality.",
  "frontend/src/components/QuestionsSection.tsx": "Chapter questions component for adding practice Q&A pairs with collapsible answer reveal and delete functionality.",
  "frontend/src/pages/QuestionBankPage.tsx": "Question bank page using backend `Question` type and `bigint` for subject/chapter filter IDs; removes the non-existent `LocalQuestion` import.",
  "frontend/src/pages/MockTestsPage.tsx": "Mock tests listing page showing all tests with attempt counts and best scores; fixed report navigation to use the correct route `/mock-tests/$testId/report` (no `$attemptId` param).",
  "frontend/src/pages/CreateMockTestPage.tsx": "Mock test creation page with test name, subject selection, and dynamic MCQ question builder with 4 options and correct answer radio selection.",
  "frontend/src/pages/AttemptMockTestPage.tsx": "Mock test attempt page showing one question at a time with option selection, timer, question navigator, and submit functionality that generates a report.",
  "frontend/src/pages/TestReportPage.tsx": "Test report page using `BigInt(testIdStr)` for the query and converting all bigint report fields (`percentage`, `score`, `total`, `timeTaken`, `attemptedAt`) to `number` before display.",
  "frontend/src/pages/ProgressPage.tsx": "Progress tracker page showing overview stats, per-subject chapter completion progress bars, planner/target completion rates, and recent mock test scores.",
  "frontend/src/components/Layout.tsx": "Authenticated app layout with a dark teal sidebar (using sidebar CSS tokens), CalendarWidget in the header, and a gradient Pro upgrade button at the bottom of the sidebar navigation. Reverts the Version 10 white sidebar back to the original dark sidebar appearance.",
  "frontend/src/pages/LoginPage.tsx": "Login/register page with Internet Identity auth flows, updated to show MadeByDevBadge below the auth card and caffeine.ai footer attribution.",
  "frontend/src/components/DailyView.tsx": "Daily planner view component that shows tasks for a specific date with add/complete/delete functionality; now correctly passes `year`, `month`, and `date` to all planner mutation hooks.",
  "frontend/src/pages/ProfilePage.tsx": "Profile page rewritten to use `useGetCallerUserProfile`/`useSaveCallerUserProfile` hooks and `getStoredUser`/`storeUser` from `useQueries`; removes all imports from the old `localStorageService` that no longer exist.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard showing ActiveTargetsSection at the top, followed by greeting, streak banner, stats grid, daily quote, subject progress bars, quick actions, and MadeByDevBadge at the bottom.",
  "frontend/src/pages/MyAchievementsPage.tsx": "Achievements page with all bigint fields from backend converted via `Number()` before passing to `formatTime`, `new Date()`, and percentage calculations.",
  "frontend/src/pages/PlannerPage.tsx": "Monthly planner calendar page; converts `task.date` (bigint nanoseconds) to milliseconds via `Number(task.date) / 1_000_000` before constructing `Date` objects.",
  "frontend/src/pages/RevisionPage.tsx": "Revision schedule page using backend `RevisionTask` type (bigint fields); converts bigint nanoseconds to ms for date display and uses `Number()` subtraction for sorting.",
  "frontend/index.html": "PWA-ready HTML entry point updated to remove all references to the non-existent `/assets/app-icon.svg` file, replacing favicon and apple-touch-icon links with valid PNG paths from the generated assets folder.",
  "frontend/public/manifest.json": "PWA web app manifest updated to remove the non-existent SVG icon entry and use only valid PNG assets from the generated folder for all icon sizes.",
  "frontend/public/sw.js": "Service worker bumped to `board-saathi-v4` with PRECACHE_URLS updated to reference the correct existing generated PNG icon files, removing old filenames that no longer exist.",
  "frontend/src/hooks/usePWAInstall.ts": "Custom React hook that captures the beforeinstallprompt event, tracks install state, provides installPrompt/dismissBanner functions, and persists banner dismissal to localStorage.",
  "frontend/src/components/PWAInstallBanner.tsx": "Fixed bottom floating PWA install banner that shows the app icon, name, and an Install button when the browser supports PWA installation; dismissible by the user.",
  "frontend/public/assets/app-icon.svg": "New decorated 💯-inspired SVG app icon featuring a deep teal-to-emerald radial gradient background, bold gold \"100\" text with glow effects, a decorative percent symbol with circles and slash, \"BOARD SAATHI\" label, sparkle/star ornaments at corners, gold ring border, corner flourishes, and shimmer gradients throughout.",
  "frontend/src/utils/localStorageService.ts": "Extended `LocalReminder` interface to include an optional `alarmSound?: string` field; all other helpers and types remain unchanged.",
  "frontend/src/hooks/useReminderAlarm.ts": "New hook that provides `playAlarm`, `previewAlarm`, and `stopAlarm` functions using the HTML Audio API, with a `ALARM_SOUNDS` map pointing to `/assets/sounds/joshsound.mp3` and a `DEFAULT_ALARM_SOUND` export.",
  "frontend/src/hooks/useReminderScheduler.ts": "New hook that accepts a list of `LocalReminder` objects and schedules two events per reminder using `setTimeout`: a browser Notification 5 minutes before the reminder time, and an alarm sound playback at the exact reminder time; requests Notification permission automatically.",
  "frontend/src/pages/RemindersPage.tsx": "Reminders page with target-linking support; removes the non-existent `isPlaying` destructure from `useReminderAlarm` and uses local `isPreviewing` state instead.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; updated useAddReminder to accept and pass optional targetId to the backend, and useGetReminders to map the targetId field from backend responses.",
  "frontend/src/components/CalendarWidget.tsx": "Calendar icon button in the header that opens a Popover showing real-time clock (updating every second), current date, days left in the month, and a shadcn Calendar with today highlighted.",
  "frontend/src/components/MadeByDevBadge.tsx": "Small reusable badge component displaying \"Made by DEV\" with a teal lightning bolt icon, used on Login and Dashboard pages.",
  "frontend/src/components/ActiveTargetsSection.tsx": "Dashboard section showing active (incomplete) targets as color-coded cards with deadline urgency badges (red/orange/green) and a Mark Complete button; shows empty state with Add Target link when no targets exist.",
  "frontend/src/pages/ProUpgradePage.tsx": "Pro upgrade page with hero section, pricing card, and feature grid describing premium Board Saathi Pro features; includes MadeByDevBadge and a placeholder upgrade CTA.",
  "frontend/src/pages/FlashcardsPage.tsx": "Flashcards page using backend `Flashcard` type (bigint IDs) instead of the removed `LocalFlashcard`; all mutations now pass `bigint` values for `cardId`, `chapterId`, and `subjectId`."
}