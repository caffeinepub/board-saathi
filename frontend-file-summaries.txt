{
  "frontend/src/App.tsx": "Main router updated to import ParentDashboard and add a /parent-dashboard route with a requireParentAuth guard (checks getParentSession()) that redirects to /login if no parent session exists; all existing student routes remain unchanged.",
  "frontend/src/pages/SubjectsPage.tsx": "Subjects listing page showing all 6 CBSE Class 10 subjects as clickable cards with chapter completion progress bars and color-coded subject icons.",
  "frontend/src/pages/ChaptersPage.tsx": "Chapter management page for a subject with add/complete chapter functionality and expandable notes/questions tabs; fixed `weightage` to pass a `number` instead of `bigint` to `useAddChapter`.",
  "frontend/src/components/NotesSection.tsx": "Chapter notes component supporting text notes and camera/file image capture, displaying notes with image preview toggle and delete functionality.",
  "frontend/src/components/QuestionsSection.tsx": "Chapter questions component for adding practice Q&A pairs with collapsible answer reveal and delete functionality.",
  "frontend/src/pages/QuestionBankPage.tsx": "Question bank page aggregating all questions across subjects/chapters with subject and chapter filter dropdowns and collapsible answer reveal.",
  "frontend/src/pages/MockTestsPage.tsx": "Mock tests listing page showing all tests with attempt counts and best scores; fixed report navigation to use the correct route `/mock-tests/$testId/report` (no `$attemptId` param).",
  "frontend/src/pages/CreateMockTestPage.tsx": "Mock test creation page with test name, subject selection, and dynamic MCQ question builder with 4 options and correct answer radio selection.",
  "frontend/src/pages/AttemptMockTestPage.tsx": "Mock test attempt page showing one question at a time with option selection, timer, question navigator, and submit functionality that generates a report.",
  "frontend/src/pages/TestReportPage.tsx": "Test report page displaying score summary and per-question breakdown; fixed `useParams` `from` path to `/layout/mock-tests/$testId/report` and now shows the latest attempt for the given test instead of requiring an `attemptId` param.",
  "frontend/src/pages/ProgressPage.tsx": "Progress tracker page showing overview stats, per-subject chapter completion progress bars, planner/target completion rates, and recent mock test scores.",
  "frontend/src/components/Layout.tsx": "Authenticated student layout updated to include a notification bell icon (with red unread count badge) in both the desktop sidebar header and mobile header; clicking opens a Popover listing all parent notifications with type-colored badges, sender info, message, and timestamp; clicking a notification marks it as read.",
  "frontend/src/pages/LoginPage.tsx": "Login/register page updated with two new parent portal buttons (\"Login as Parent\" and \"Create Parent Account\") below the guest mode option, each opening a dedicated Dialog with appropriate form fields and validation; parent login redirects to /parent-dashboard on success.",
  "frontend/src/components/DailyView.tsx": "Daily planner view component that shows tasks for a specific date with add/complete/delete functionality; now correctly passes `year`, `month`, and `date` to all planner mutation hooks.",
  "frontend/src/pages/ProfilePage.tsx": "Updated Profile page with a new \"Upgrade App\" card at the bottom that posts a `SKIP_WAITING` message to the service worker, forces an update check, and reloads the app to apply the latest cached version.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page showing study stats, streak, subject progress, and quick links; now reads user info directly from localStorage service instead of the removed `getStoredAuth` helper.",
  "frontend/src/pages/MyAchievementsPage.tsx": "Achievements page showing personal best stats, rank, streak badges, and subject scores; fixed `formatTime` to accept `number` instead of `bigint` since `fastestTestTime` is now a plain number.",
  "frontend/src/pages/PlannerPage.tsx": "Monthly calendar planner page with task counts per day; refactored to use `number` (ms) timestamps instead of `bigint` nanoseconds, and passes `year/month/date` numbers to `DailyView` instead of a `Date` object.",
  "frontend/src/pages/RevisionPage.tsx": "Spaced-repetition revision schedule page; fully rewritten to use `LocalRevisionTask` (number IDs, ms timestamps) instead of the backend `RevisionTask` type, with chapter name lookup and proper toggle handling.",
  "frontend/index.html": "PWA-ready HTML entry point updated to remove all references to the non-existent `/assets/app-icon.svg` file, replacing favicon and apple-touch-icon links with valid PNG paths from the generated assets folder.",
  "frontend/public/manifest.json": "PWA web app manifest updated to remove the non-existent SVG icon entry and use only valid PNG assets from the generated folder for all icon sizes.",
  "frontend/public/sw.js": "Service worker upgraded to `board-saathi-v5` with a comprehensive fetch handler that caches all JS, CSS, images, fonts, audio, and HTML assets on first load, serves from cache-first for all same-origin GET requests, skips ICP `/api/` calls, and falls back to `index.html` for offline navigation — enabling full offline functionality after the first visit.",
  "frontend/src/hooks/usePWAInstall.ts": "Custom React hook that captures the beforeinstallprompt event, tracks install state, provides installPrompt/dismissBanner functions, and persists banner dismissal to localStorage.",
  "frontend/src/components/PWAInstallBanner.tsx": "Fixed bottom floating PWA install banner that shows the app icon, name, and an Install button when the browser supports PWA installation; dismissible by the user.",
  "frontend/public/assets/app-icon.svg": "New decorated 💯-inspired SVG app icon featuring a deep teal-to-emerald radial gradient background, bold gold \"100\" text with glow effects, a decorative percent symbol with circles and slash, \"BOARD SAATHI\" label, sparkle/star ornaments at corners, gold ring border, corner flourishes, and shimmer gradients throughout.",
  "frontend/src/utils/localStorageService.ts": "Extended localStorage service with parent portal support — adds parent account creation/login/session management (createParentAccount, parentLogin, getParentSession, setParentSession, clearParentSession, getLinkedChildData) and a full notification system (addNotification, getNotifications, markNotificationRead, getUnreadNotificationCount) for parent-to-child messaging.",
  "frontend/src/hooks/useReminderAlarm.ts": "New hook that provides `playAlarm`, `previewAlarm`, and `stopAlarm` functions using the HTML Audio API, with a `ALARM_SOUNDS` map pointing to `/assets/sounds/joshsound.mp3` and a `DEFAULT_ALARM_SOUND` export.",
  "frontend/src/hooks/useReminderScheduler.ts": "New hook that accepts a list of `LocalReminder` objects and schedules two events per reminder using `setTimeout`: a browser Notification 5 minutes before the reminder time, and an alarm sound playback at the exact reminder time; requests Notification permission automatically.",
  "frontend/src/pages/RemindersPage.tsx": "Updated Reminders page that integrates `useReminderScheduler` for automatic scheduling, adds an Alarm Sound dropdown (defaulting to JoshSound) with a preview/stop button in the Add Reminder dialog, and persists the selected sound with each reminder.",
  "frontend/src/hooks/useQueries.ts": "Updated `useAddReminder` mutation to accept and persist an optional `alarmSound` field alongside `text` and `dateTime`; all other hooks remain unchanged.",
  "frontend/src/pages/ParentDashboard.tsx": "New standalone parent dashboard page showing the parent's username and linked child's name in the header, with collapsible sections for subjects/chapters progress, mock tests (attempted/not attempted status), flashcards (learned count), notes, question bank, and an overall performance summary; includes a prominent comment box with Scold/Comment/Appreciate tone buttons that send notifications to the child."
}