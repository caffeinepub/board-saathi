{
  "frontend/src/App.tsx": "Main TanStack Router configuration with all routes, plus a `registerServiceWorker()` function that registers `/sw.js`, listens for `controllerchange` to auto-reload on new SW activation, and triggers `SKIP_WAITING` when a new worker installs.",
  "frontend/src/pages/SubjectsPage.tsx": "Subjects listing page showing all 6 CBSE Class 10 subjects as clickable cards with chapter completion progress bars and color-coded subject icons.",
  "frontend/src/pages/ChaptersPage.tsx": "Chapter management page for a subject with add/complete chapter functionality and expandable notes/questions tabs; fixed `weightage` to pass a `number` instead of `bigint` to `useAddChapter`.",
  "frontend/src/components/NotesSection.tsx": "Chapter notes component supporting text notes and camera/file image capture, displaying notes with image preview toggle and delete functionality.",
  "frontend/src/components/QuestionsSection.tsx": "Chapter questions component for adding practice Q&A pairs with collapsible answer reveal and delete functionality.",
  "frontend/src/pages/QuestionBankPage.tsx": "Question bank page aggregating all questions across subjects/chapters with subject and chapter filter dropdowns and collapsible answer reveal.",
  "frontend/src/pages/MockTestsPage.tsx": "Mock tests listing page showing all tests with attempt counts and best scores; fixed report navigation to use the correct route `/mock-tests/$testId/report` (no `$attemptId` param).",
  "frontend/src/pages/CreateMockTestPage.tsx": "Mock test creation page with test name, subject selection, and dynamic MCQ question builder with 4 options and correct answer radio selection.",
  "frontend/src/pages/AttemptMockTestPage.tsx": "Mock test attempt page with timer, option selection, and question navigator; fixed `handleSubmit` to pass `answers` as `Record<number, number>` directly instead of mapping to an array.",
  "frontend/src/pages/TestReportPage.tsx": "Test report page displaying score summary and per-question breakdown; fixed `useParams` `from` path to `/layout/mock-tests/$testId/report` and now shows the latest attempt for the given test instead of requiring an `attemptId` param.",
  "frontend/src/pages/ProgressPage.tsx": "Progress tracker page showing overview stats, per-subject chapter completion progress bars, planner/target completion rates, and recent mock test scores.",
  "frontend/src/components/Layout.tsx": "Authenticated student layout with desktop sidebar and mobile drawer; now tracks `navigator.onLine` via `window` online/offline events and renders a fixed teal banner at the top of the screen when the device is offline, automatically dismissing it when connectivity is restored.",
  "frontend/src/pages/LoginPage.tsx": "Login/register page for students and parents, now integrated with the global backend canister — student registration calls `actor.registerStudent()` to save accounts globally, parent login checks `actor.getParentByUsername()` first before falling back to localStorage, and parent registration calls `actor.registerParent()` after verifying the child account locally; all flows gracefully fall back to localStorage with informative toasts when the backend is unreachable.",
  "frontend/src/components/DailyView.tsx": "Daily planner view component that shows tasks for a specific date; fixed `useAddPlannerTask` call to pass `year`, `month`, `date` as separate fields instead of a computed `date` timestamp.",
  "frontend/src/pages/ProfilePage.tsx": "Profile page with edit profile form, change password form, and app update button; uses saveUserAccount and isGuest from the restored localStorageService, with optional createdAt display.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page with days-left banner, TargetMotivationWidget, quick stats, upcoming reminders/targets sections, and CalendarClock icon linking to /timer; uses LocalTarget/LocalReminder types with number fields.",
  "frontend/src/pages/MyAchievementsPage.tsx": "Achievements page showing personal best stats, rank, streak badges, and subject scores; fixed `formatTime` to accept `number` instead of `bigint` since `fastestTestTime` is now a plain number.",
  "frontend/src/pages/PlannerPage.tsx": "Monthly calendar planner page with task counts per day; refactored to use `number` (ms) timestamps instead of `bigint` nanoseconds, and passes `year/month/date` numbers to `DailyView` instead of a `Date` object.",
  "frontend/src/pages/RevisionPage.tsx": "Spaced-repetition revision schedule page; fully rewritten to use `LocalRevisionTask` (number IDs, ms timestamps) instead of the backend `RevisionTask` type, with chapter name lookup and proper toggle handling.",
  "frontend/index.html": "PWA-ready HTML entry point updated to use the new DEV-branded study icons — favicon now points to `favicon.dim_32x32.png`, apple-touch-icon uses `app-icon-192.dim_192x192.png`, and all icon references use the latest generated assets.",
  "frontend/public/manifest.json": "PWA web app manifest updated to include the new 32x32 favicon entry and reference the fresh DEV-branded icon set (192x192, 512x512 any, 512x512 maskable) from the generated assets folder.",
  "frontend/public/sw.js": "Fully rewritten service worker (board-saathi-v7) with separate STATIC_CACHE and DYNAMIC_CACHE, precaching of all static assets and /offline.html during install, old cache cleanup on activate, cache-first strategy for static assets, network-first with offline fallback for navigation requests, and a SKIP_WAITING message handler.",
  "frontend/src/hooks/usePWAInstall.ts": "Custom React hook that captures the beforeinstallprompt event, tracks install state, provides installPrompt/dismissBanner functions, and persists banner dismissal to localStorage.",
  "frontend/src/components/PWAInstallBanner.tsx": "Fixed bottom floating PWA install banner that shows the app icon, name, and an Install button when the browser supports PWA installation; dismissible by the user.",
  "frontend/public/assets/app-icon.svg": "New decorated 💯-inspired SVG app icon featuring a deep teal-to-emerald radial gradient background, bold gold \"100\" text with glow effects, a decorative percent symbol with circles and slash, \"BOARD SAATHI\" label, sparkle/star ornaments at corners, gold ring border, corner flourishes, and shimmer gradients throughout.",
  "frontend/src/utils/localStorageService.ts": "Complete localStorage service restoring all original exports (StoredUserAccount, all CRUD helpers, auth, parent portal, notifications, messaging) plus new TimerData interface and timer functions (getTimers, addTimer, deleteTimer). Also adds getStoredAuth alias and fixes markMessageAsRead/updateMessageReply/saveChildReplyToParent/markParentReplyAsSeen signatures to use string IDs.",
  "frontend/src/hooks/useReminderAlarm.ts": "Hook for playing alarm sounds using the HTML Audio API, with a documented ALARM_SOUNDS map pointing to `/assets/sounds/joshsound.mp3` and a comment listing all sound paths for service worker precaching reference.",
  "frontend/src/hooks/useReminderScheduler.ts": "Hook that accepts LocalReminder objects and schedules two events per reminder: a browser notification 5 minutes before the reminder time, and both an alarm sound playback AND a \"due now\" browser notification at the exact reminder time; requests Notification permission automatically and cleans up timeouts on unmount.",
  "frontend/src/pages/RemindersPage.tsx": "Reminders page with alarm scheduling integration; each upcoming reminder card now shows an \"Alert: 5 min before + alarm at due time\" info label with an AlarmClock icon, and the add dialog also shows this scheduling info.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for localStorage-based backend operations; fixed the two call sites at lines 746 and 770 where markMessageAsReadLS and markParentReplyAsSeenLS were incorrectly called with two arguments — now correctly called with only the ID argument matching the updated localStorageService signatures.",
  "frontend/src/pages/ParentDashboard.tsx": "Fully fixed parent dashboard that correctly reads the parent session from `sessionStorage` via `getParentSession()` and loads child data via `getLinkedChildData()` from `localStorageService`, replacing the broken `localStorage.getItem('parent_session')` call and mismatched data keys. Renders hero banner, feedback panel, live chat, attendance/streak, grade performance, smart alerts, upcoming events, and behavior/progress cards using the properly typed `ParentSession` and `LinkedChildData` types.",
  "frontend/src/pages/StudentMessagesPage.tsx": "Student-facing messages page with two tabs — Feedback (showing color-coded Comment/Appreciate/Scold messages from parent with mark-as-read) and Live Chat (bidirectional chat with parent using localStorage polling every 3 seconds).",
  "frontend/src/hooks/useTargetAlarmScheduler.ts": "New custom hook that accepts a list of targets and schedules a 1-hour-before alarm (sound + browser notification) for each active target with a future deadline; uses useReminderAlarm for sound playback, requests notification permission, and cleans up all timeouts on unmount.",
  "frontend/src/pages/TargetsPage.tsx": "Targets page that now integrates useTargetAlarmScheduler for automatic 1-hour-before deadline alarms; active target cards show a \"⏰ 1hr alarm set\" badge when the deadline is more than 1 hour away, and the add dialog informs users about the alarm feature.",
  "frontend/src/pages/TimerPage.tsx": "New countdown timer page allowing users to add multiple named timers with start (month/day) and end (year/month/day) dates; displays live countdowns in months/days/hours/minutes with real-time updates via setInterval, persisted to localStorage.",
  "frontend/src/components/BottomNavBar.tsx": "Fixed bottom navigation bar with five buttons (Home, Subject, Question, Reminder, Target) using lucide-react icons, TanStack Router Links, and active route highlighting via useRouterState.",
  "frontend/src/components/TargetMotivationWidget.tsx": "Motivational target widget using LocalTarget (number IDs) instead of backend Target type, displaying completion percentage with gold gradient design, circular SVG progress, trophy icon, and dynamic motivational messages.",
  "frontend/public/offline.html": "Self-contained offline fallback page with inline CSS using the teal/orange brand palette, an animated offline badge, a friendly message, a \"Try Again\" reload button, and a list of features available while offline — no external dependencies."
}